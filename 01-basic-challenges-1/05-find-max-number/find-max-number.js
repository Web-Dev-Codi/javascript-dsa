function findMaxNumber(arr) {

    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === arr[i]) {
            console.log([i].sort());
        }

    }
}

module.exports = findMaxNumber;
